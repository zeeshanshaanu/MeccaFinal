import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme) => ({
  text: {
    color: "#ffffff",
    fontSize: "20px",
  },
  icons: {
    color: "#ffffff",
    paddingRight: "1.5rem",
  },
}));
export default useStyles;
